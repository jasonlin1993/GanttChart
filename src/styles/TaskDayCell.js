// styles/TaskDayCell.js
import styled from "styled-components";

const TaskDayCell = styled.div`
  flex: 1; // 使所有任务日单元格在容器中均匀分布
  border-right: 1px solid #ddd; // 添加右边界以创建分割线
`;

export default TaskDayCell;
